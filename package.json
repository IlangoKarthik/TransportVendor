{
  "name": "netkathir-ai-tool",
  "version": "2.0.0",
  "description": "Unified AI platform with document search, vendor management, and AI search",
  "scripts": {
    "start": "concurrently \"npm run flask\" \"npm run server\" \"npm run client\"",
    "flask": "./start-flask-services.sh",
    "server": "cd server && npm start",
    "client": "cd client && npm start",
    "build": "cd client && npm run build",
    "install-all": "cd server && npm install && cd ../client && npm install",
    "stop-flask": "./stop-flask-services.sh"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
